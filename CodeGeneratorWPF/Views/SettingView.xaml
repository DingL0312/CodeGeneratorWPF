<UserControl x:Class="CodeGeneratorWPF.Views.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodeGeneratorWPF.Views"
             xmlns:vm="clr-namespace:CodeGeneratorWPF.ViewModel"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:SettingViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Left">
            <Button Content="返回"  Width="40" Margin="10,0,0,0" Height="20" Command="{Binding BackClickCommand}"/>
        </Grid>
        <Grid Grid.Row="1">
            <TabControl>
                <TabItem Header="实体类设置" ></TabItem>
                <TabItem Header="1" ></TabItem>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>
